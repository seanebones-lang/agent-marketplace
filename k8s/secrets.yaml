# This is a template. Replace with actual secrets in production.
# Use: kubectl create secret generic agent-marketplace-secrets --from-literal=...

apiVersion: v1
kind: Secret
metadata:
  name: agent-marketplace-secrets
  namespace: agent-marketplace
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:password@postgres:5432/agentic_db"
  REDIS_URL: "redis://redis:6379/0"
  SECRET_KEY: "your-secret-key-change-in-production"
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  GROQ_API_KEY: ""
  QDRANT_API_KEY: ""

